namespace = formable_heritage 

# Formable Heritages
formable_heritage.1 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.1.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.1.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = LNK
    }

    immediate = {
		save_scope_as = lankan_country
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.1.a # LOCALIZATION KEY - Option A text
        hidden_effect = {
				set_country_heritage = lankan_heritage # EFFECT
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = lankan_modifier
				duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.1.b # LOCALIZATION KEY - Option B text
        add_political_influence = 75
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.1.c # LOCALIZATION KEY - Option B text
		hidden_effect = {
			set_country_heritage = clan_heritage
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = clan_modifier
				duration = -1
			}
		}
	}	
}

formable_heritage.2 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.2.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.2.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = NUB
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.2.a # LOCALIZATION KEY - Option A text
        hidden_effect = {
			set_country_heritage = nubian_heritage # EFFECT
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = nubian_modifier
				duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.2.b # LOCALIZATION KEY - Option B text
        add_political_influence = 75
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.2.c # LOCALIZATION KEY - Option B text
		hidden_effect = {
			set_country_heritage = clan_heritage
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = clan_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.3 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.3.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.3.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = BOD
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.3.a # LOCALIZATION KEY - Option A text
		hidden_effect = {
			set_country_heritage = himalayan_heritage # EFFECT
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = himalayan_modifier
				duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.3.b # LOCALIZATION KEY - Option B text
        add_political_influence = 75
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.3.c # LOCALIZATION KEY - Option B text
		hidden_effect = {
			set_country_heritage = clan_heritage
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = clan_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.4 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.4.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.4.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = SAK
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.4.a # LOCALIZATION KEY - Option A text
		hidden_effect = {
			set_country_heritage = sakastani_heritage # EFFECT
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = sakastani_modifier
				duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.4.b # LOCALIZATION KEY - Option B text
        add_political_influence = 75
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.4.c # LOCALIZATION KEY - Option B text
		hidden_effect = {
			set_country_heritage = clan_heritage
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = clan_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.5 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.5.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.5.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = DPR
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.5.a # LOCALIZATION KEY - Option A text
		hidden_effect = {
			set_country_heritage = danaprisan_heritage # EFFECT
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = danaprisan_modifier
				duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.5.b # LOCALIZATION KEY - Option B text
        add_political_influence = 75
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.5.c # LOCALIZATION KEY - Option B text
		hidden_effect = {
			set_country_heritage = clan_heritage
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = clan_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.6 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.6.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.6.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = ETP
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.6.a # LOCALIZATION KEY - Option A text
		hidden_effect = {
			set_country_heritage = aethiopian_heritage # EFFECT
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = aethiopian_modifier
				duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.6.b # LOCALIZATION KEY - Option B text
        add_political_influence = 75
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.6.c # LOCALIZATION KEY - Option B text
		hidden_effect = {
			set_country_heritage = clan_heritage
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = clan_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.7 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.7.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.7.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = DMK
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.7.a # LOCALIZATION KEY - Option A text
		hidden_effect = {
			set_country_heritage = danmorker_heritage # EFFECT
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = danmorker_modifier
				duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.7.b # LOCALIZATION KEY - Option B text
        add_political_influence = 75
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.7.c # LOCALIZATION KEY - Option B text
		hidden_effect = {
			set_country_heritage = clan_heritage
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = clan_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.8 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.8.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.8.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = HND
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.8.a # LOCALIZATION KEY - Option A text
		hidden_effect = {
			set_country_heritage = hindi_heritage # EFFECT
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = hindi_modifier
				duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.8.b # LOCALIZATION KEY - Option B text
        add_political_influence = 75
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.8.c # LOCALIZATION KEY - Option B text
		hidden_effect = {
			set_country_heritage = clan_heritage
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = clan_modifier
				#duration = -1
			}
		}		
	}
}

formable_heritage.9 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.9.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.9.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = KNK
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.9.a # LOCALIZATION KEY - Option A text
		hidden_effect = {
			set_country_heritage = konkani_heritage # EFFECT
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = konkani_modifier
				duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.9.b # LOCALIZATION KEY - Option B text
        add_political_influence = 75
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.9.c # LOCALIZATION KEY - Option B text
		hidden_effect = {
			set_country_heritage = clan_heritage
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = clan_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.10 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.10.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.10.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = ARN
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.10.a # LOCALIZATION KEY - Option A text
		hidden_effect = {
			set_country_heritage = arvasthani_heritage # EFFECT
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = arvasthani_modifier
				duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.10.b # LOCALIZATION KEY - Option B text
        add_political_influence = 75
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.10.c # LOCALIZATION KEY - Option B text
		hidden_effect = {
			set_country_heritage = clan_heritage
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = clan_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.11 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.11.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.11.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = GNG
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.11.a # LOCALIZATION KEY - Option A text
		hidden_effect = {
			set_country_heritage = gangaridi_heritage # EFFECT
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = gangaridi_modifier
				duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.11.b # LOCALIZATION KEY - Option B text
        add_political_influence = 75
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.11.c # LOCALIZATION KEY - Option B text
		hidden_effect = {
			set_country_heritage = clan_heritage
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = clan_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.12 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.12.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.12.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = ASI
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.12.a # LOCALIZATION KEY - Option A text
		hidden_effect = {
			set_country_heritage = asian_heritage # EFFECT
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = asian_modifier
				duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.12.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }	
}

formable_heritage.13 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.13.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.13.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = BHK
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.13.a # LOCALIZATION KEY - Option A text
		hidden_effect = {
			set_country_heritage = balhikan_heritage # EFFECT
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = balhikan_modifier
				duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.13.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.13.c # LOCALIZATION KEY - Option B text
		hidden_effect = {
			set_country_heritage = tribal_heritage
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				duration = -1
			}
		}
	}	
}

formable_heritage.14 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.14.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.14.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = EZY
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.14.a # LOCALIZATION KEY - Option A text
		hidden_effect = {
			set_country_heritage = israelite_heritage # EFFECT
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = israelite_modifier
				duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.14.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }	
}

formable_heritage.15 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.15.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.15.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = KST
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.15.a # LOCALIZATION KEY - Option A text
		hidden_effect = {
			set_country_heritage = kshaharatan_heritage # EFFECT
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = kshaharatan_modifier
				duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.15.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.15.c # LOCALIZATION KEY - Option B text
		hidden_effect = {
			set_country_heritage = tribal_heritage
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				duration = -1
			}
		}
	}	
}

formable_heritage.16 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.16.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.16.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = MJL
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.16.a # LOCALIZATION KEY - Option A text
		hidden_effect = {
			set_country_heritage = mahajanapadan_heritage # EFFECT
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = mahajanapadan_modifier
				duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.16.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }	
}

formable_heritage.17 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.17.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.17.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = MGV
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.17.a # LOCALIZATION KEY - Option A text
		hidden_effect = {
			set_country_heritage = mahameghavahani_heritage # EFFECT
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = mahameghavahani_modifier
				duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.17.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.17.c # LOCALIZATION KEY - Option B text
		hidden_effect = {
			set_country_heritage = tribal_heritage
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				duration = -1
			}
		}
	}	
}

formable_heritage.18 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.18.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.18.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = MWA
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.18.a # LOCALIZATION KEY - Option A text
		hidden_effect = {
			set_country_heritage = malavi_heritage # EFFECT
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = malavi_modifier
				duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.18.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.18.c # LOCALIZATION KEY - Option B text
		hidden_effect = {
			set_country_heritage = tribal_heritage
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				duration = -1
			}
		}
	}	
}

formable_heritage.19 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.19.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.19.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = NLK
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.19.a # LOCALIZATION KEY - Option A text
		hidden_effect = {
			set_country_heritage = neo_lydian_heritage # EFFECT
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = neo_lydian_modifier
				duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.19.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.19.c # LOCALIZATION KEY - Option B text
		hidden_effect = {
			set_country_heritage = tribal_heritage
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				duration = -1
			}
		}
	}	
}

formable_heritage.20 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.20.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.20.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = NPL
	}	

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.20.a # LOCALIZATION KEY - Option A text
		hidden_effect = {
			set_country_heritage = nepali_heritage # EFFECT
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = nepali_modifier
				duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.20.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.20.c # LOCALIZATION KEY - Option B text
		hidden_effect = {
			set_country_heritage = tribal_heritage
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				duration = -1
			}
		}
	}	
}

formable_heritage.21 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.21.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.21.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = SGA
	}	

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.21.a # LOCALIZATION KEY - Option A text
		hidden_effect = {
			set_country_heritage = shungi_heritage # EFFECT
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = shungi_modifier
				duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.21.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.21.c # LOCALIZATION KEY - Option B text
		hidden_effect = {
			set_country_heritage = tribal_heritage
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				duration = -1
			}
		}
	}	
}

formable_heritage.22 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.22.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.22.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = SRN
	}	

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.22.a # LOCALIZATION KEY - Option A text
		hidden_effect = {
			set_country_heritage = sureni_heritage # EFFECT
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = sureni_modifier
				duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.22.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.22.c # LOCALIZATION KEY - Option B text
		hidden_effect = {
			set_country_heritage = tribal_heritage
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				duration = -1
			}
		}
	}	
}

formable_heritage.23 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.23.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.23.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = TOK
	}		

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.23.a # LOCALIZATION KEY - Option A text
		hidden_effect = {
			set_country_heritage = tokharian_heritage # EFFECT
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tokharian_modifier
				duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.23.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.23.c # LOCALIZATION KEY - Option B text
		hidden_effect = {
			set_country_heritage = tribal_heritage
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				duration = -1
			}
		}
	}	
}

formable_heritage.24 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.24.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.24.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = YRY
	}	
	
    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.24.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = yavani_heritage 
		}	# EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = yavani_modifier
				duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.24.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }	
}

formable_heritage.25 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.25.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.25.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = KVK
	}
	
    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.25.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = kavkasi_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = kavkasi_modifier
				duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.25.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.25.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = tribal_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				duration = -1
			}
		}
	}	
}

formable_heritage.26 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.26.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.26.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = HKL
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.26.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = heraklean_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = heraklean_modifier
				duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.26.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }	
}

formable_heritage.27 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.27.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.27.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = ISP
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.27.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = ispanian_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = ispanian_modifier
				duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.27.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }	
}

formable_heritage.28 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.28.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.28.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = GLO
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.28.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = glorious_olbian_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = glorious_olbian_modifier
				duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.28.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
}

formable_heritage.29 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.29.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.29.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = PUN
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.29.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = puntic_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = puntic_modifier
				duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.29.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.29.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = tribal_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				duration = -1
			}
		}
	}	
}

formable_heritage.30 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.30.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.30.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = LAI
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.30.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = laietanian_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = laietanian_modifier
				duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.30.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.30.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = tribal_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				duration = -1
			}
		}
	}	
}

formable_heritage.31 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.31.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.31.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = RHD
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.31.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = rhodanian_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = rhodanian_modifier
				duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.31.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.31.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = tribal_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				duration = -1
			}
		}
	}	
}

formable_heritage.32 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.32.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.32.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = BRY
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.32.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = hibernia_magna_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = hibernia_magna_modifier
				duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.32.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.32.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = tribal_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				duration = -1
			}
		}
	}	
}

formable_heritage.33 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.33.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.33.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = AEN
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.33.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = aenglander_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = aenglander_modifier
				duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.33.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.33.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = tribal_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				duration = -1
			}
		}
	}	
}

formable_heritage.34 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.34.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.34.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = SCN
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.34.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = scandian_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = scandian_modifier
				duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.34.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.34.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = tribal_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				duration = -1
			}
		}
	}	
}

formable_heritage.35 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.35.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.35.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = MCB
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.35.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = macrobian_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = macrobian_modifier
				duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.35.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.35.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = tribal_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				duration = -1
			}
		}
	}	
}

formable_heritage.36 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.36.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.36.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = GLP
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.36.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = lepontic_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = lepontic_modifier
				duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.36.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.36.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = tribal_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				duration = -1
			}
		}
	}	
}

formable_heritage.37 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.37.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.37.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = EUS
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.37.a # LOCALIZATION KEY - Option A text
        hidden_effect = { set_country_heritage = euskara_heritage } # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = euskara_modifier
				duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.37.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.37.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = tribal_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				duration = -1
			}
		}
	}	
}

formable_heritage.38 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.38.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.38.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = ANA
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.38.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = anatolian_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = anatolian_modifier
				duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.38.b # LOCALIZATION KEY - Option B text
        add_political_influence = 200
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.38.c # LOCALIZATION KEY - Option B text
		hidden_effect = { set_country_heritage = horde_heritage }
		show_as_tooltip = {
			add_country_modifier = {
				name = horde_modifier
				duration = -1
			}
		}
	}	
}

formable_heritage.39 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.39.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.39.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = ARB
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.39.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = arabian_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = arabian_modifier
				duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.39.b # LOCALIZATION KEY - Option B text
        add_political_influence = 200
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.39.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = horde_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = horde_modifier
				duration = -1
			}
		}
	}	
}

formable_heritage.40 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.40.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.40.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = ARY
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.40.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = aryan_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = aryan_modifier
				duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.40.b # LOCALIZATION KEY - Option B text
        add_political_influence = 200
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.40.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = horde_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = horde_modifier
				duration = -1
			}
		}
	}	
}

formable_heritage.41 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.41.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.41.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = ERE
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.41.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = imperial_erythraean_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = imperial_erythraean_modifier
				duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.41.b # LOCALIZATION KEY - Option B text
        add_political_influence = 200
    }
}

formable_heritage.42 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.42.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.42.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = ERL
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.42.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = republican_erythraean_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = republican_erythraean_modifier
				duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.42.b # LOCALIZATION KEY - Option B text
        add_political_influence = 200
    }
}

formable_heritage.43 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.43.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.43.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = EUE
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.43.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = imperial_euxine_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = imperial_euxine_modifier
				duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.43.b # LOCALIZATION KEY - Option B text
        add_political_influence = 200
    }
}

formable_heritage.44 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.44.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.44.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = EUL
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.44.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = republican_euxine_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = republican_euxine_modifier
				duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.44.b # LOCALIZATION KEY - Option B text
        add_political_influence = 200
    }
}

formable_heritage.45 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.45.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.45.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = LBA
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.45.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = libyan_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = libyan_modifier
				duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.45.b # LOCALIZATION KEY - Option B text
        add_political_influence = 200
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.45.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = horde_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = horde_modifier
				duration = -1
			}
		}
	}	
}

formable_heritage.46 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.46.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.46.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = NEA
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.46.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = neo_assyrian_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = neo_assyrian_modifier
				duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.46.b # LOCALIZATION KEY - Option B text
        add_political_influence = 200
    }
}

formable_heritage.47 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.47.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.47.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = IND
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.47.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = indian_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = indian_modifier
				duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.47.b # LOCALIZATION KEY - Option B text
        add_political_influence = 200
    }
}

formable_heritage.48 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.48.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.48.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = CEL
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.48.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = celtic_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = celtic_modifier
				duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.48.b # LOCALIZATION KEY - Option B text
        add_political_influence = 200
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.48.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = horde_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = horde_modifier
				duration = -1
			}
		}
	}	
}

formable_heritage.49 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.49.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.49.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = GEE
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.49.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = germanic_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = germanic_modifier
				duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.49.b # LOCALIZATION KEY - Option B text
        add_political_influence = 200
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.49.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = horde_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = horde_modifier
				duration = -1
			}
		}
	}	
}

formable_heritage.50 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.50.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.50.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = GAM
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.50.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = greater_armenian_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = greater_armenian_modifier
				duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.50.b # LOCALIZATION KEY - Option B text
        add_political_influence = 200
    }
}

formable_heritage.51 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.51.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.51.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = TEE
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.51.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = tyrrhenian_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = tyrrhenian_modifier
				duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.51.b # LOCALIZATION KEY - Option B text
        add_political_influence = 200
    }
}

formable_heritage.52 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.52.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.52.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = OKE
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.52.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = heritage_of_the_oikumene 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = modifier_of_the_oikumene
				duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.52.b # LOCALIZATION KEY - Option B text
        add_political_influence = 200
    }
}

formable_heritage.53 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.53.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.53.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = CBR
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.53.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = celtiberian_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = celtiberian_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.53.b # LOCALIZATION KEY - Option B text
        add_political_influence = 75
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.53.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = clan_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = clan_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.54 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.54.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.54.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = ITL
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.54.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = italian_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.54.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.54.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = tribal_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.55 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.55.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.55.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = SAT
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.55.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = satavahani_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = satavahani_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.55.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.55.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = tribal_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.56 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.56.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.56.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = NUM
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.56.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = numidian_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = numidian_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.56.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.56.c # LOCALIZATION KEY - Option B text
		hidden_effect = {
			set_country_heritage = tribal_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.57 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.57.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.57.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = AQI
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.57.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = aquitanian_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = aquitanian_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.57.b # LOCALIZATION KEY - Option B text
        add_political_influence = 75
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.57.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = clan_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = clan_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.58 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.58.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.58.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = ASR
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.58.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = assyrian_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = assyrian_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.58.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
}

formable_heritage.59 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.59.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.59.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = BBY
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.59.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = babylonian_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = babylonian_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.59.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
}

formable_heritage.60 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.60.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.60.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = BGG
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.60.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = heritage_of_the_belgae 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = modifier_of_the_belgae 
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.60.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.60.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = tribal_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.61 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.61.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.61.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = DAC
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.61.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = dacian_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = dacian_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.61.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.61.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = tribal_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.62 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.62.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.62.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = GAU
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.62.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = gallic_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = gallic_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.62.b # LOCALIZATION KEY - Option B text
        add_political_influence = 200
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.62.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = horde_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = horde_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.63 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.63.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.63.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = ILL
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.63.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = illyrian_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = illyrian_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.63.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.63.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = tribal_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.64 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.64.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.64.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = MEE
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.64.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = median_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = median_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.64.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.64.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = tribal_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.65 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.65.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.65.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = NRM
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.65.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = noric_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = noric_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.65.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.65.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = tribal_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.66 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.66.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.66.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = YMN
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.66.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = yemeni_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = yemeni_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.66.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.66.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = tribal_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.67 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.67.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.67.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = SYA
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.67.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = syrian_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = syrian_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.67.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.67.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = tribal_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.68 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.68.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.68.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = DRA
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.68.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = dravidian_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = dravidian_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.68.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.68.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = tribal_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.69 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.69.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.69.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = HVL
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.69.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = helvetian_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = helvetian_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.69.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.69.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = tribal_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.70 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.70.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.70.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = PRG
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.70.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = phrygian_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = phrygian_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.70.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.70.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = tribal_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.71 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.71.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.71.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = BRA
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.71.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = pritanian_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = pritanian_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.71.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.71.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = tribal_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.72 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.72.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.72.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = CCC
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.72.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = caledonian_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = caledonian_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.72.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.72.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = tribal_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.73 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.73.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.73.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = HIB
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.73.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = hibernian_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = hibernian_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.73.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.73.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = tribal_heritage
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.74 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.74.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.74.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = SAR
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.74.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = sardinian_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = sardinian_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.74.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.74.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = tribal_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.75 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.75.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.75.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = XXS
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.75.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = saxon_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = saxon_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.75.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.75.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = tribal_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.76 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.76.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.76.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = SII
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.76.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = sicilian_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = sicilian_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.76.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.76.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = tribal_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.77 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.77.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.77.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = HBR
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.77.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = greater_iberian_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = greater_iberian_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.77.b # LOCALIZATION KEY - Option B text
        add_political_influence = 200
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.77.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = horde_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = horde_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.78 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.78.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.78.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = AUA
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.78.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = aestuian_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = aestuian_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.78.b # LOCALIZATION KEY - Option B text
        add_political_influence = 75
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.78.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = clan_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = clan_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.79 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.79.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.79.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = ASX
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.79.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = armorican_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = armorican_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.79.b # LOCALIZATION KEY - Option B text
        add_political_influence = 75
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.79.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = clan_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = clan_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.80 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.80.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.80.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = ATU
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.80.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = asturian_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = asturian_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.80.b # LOCALIZATION KEY - Option B text
        add_political_influence = 75
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.80.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = clan_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = clan_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.81 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.81.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.81.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = CNN
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.81.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = cantabrian_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = cantabrian_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.81.b # LOCALIZATION KEY - Option B text
        add_political_influence = 75
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.81.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = clan_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = clan_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.82 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.82.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.82.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = CPT
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.82.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = carpetanian_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = carpetanian_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.82.b # LOCALIZATION KEY - Option B text
        add_political_influence = 75
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.82.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 	
			set_country_heritage = clan_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = clan_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.83 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.83.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.83.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = GLL
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.83.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = gallaecian_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = gallaecian_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.83.b # LOCALIZATION KEY - Option B text
        add_political_influence = 75
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.83.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = clan_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = clan_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.84 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.84.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.84.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = LST
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.84.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = lusitanian_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = lusitanian_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.84.b # LOCALIZATION KEY - Option B text
        add_political_influence = 75
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.84.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = clan_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = clan_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.85 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.85.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.85.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = SBN
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.85.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 	
			set_country_heritage = suebian_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = suebian_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.85.b # LOCALIZATION KEY - Option B text
        add_political_influence = 75
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.85.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = clan_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = clan_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.86 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.86.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.86.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = VTT
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.86.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = vettonian_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = vettonian_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.86.b # LOCALIZATION KEY - Option B text
        add_political_influence = 75
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.86.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = clan_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = clan_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.87 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.87.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.87.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = CCI
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.87.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = cilician_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = cilician_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.87.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.87.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = tribal_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.88 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.88.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.88.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = AIO
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.88.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = heritage_of_albion # EFFECT
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = modifier_of_albion
				#duration = -1
			}
		}
	}
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.88.b # LOCALIZATION KEY - Option B text
        add_political_influence = 200
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.88.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = horde_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = horde_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.89 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.89.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.89.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = EGT
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.89.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = egyptian_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = egyptian_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.89.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.89.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = tribal_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.90 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.90.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.90.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = MGG
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.90.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = magna_graecian_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = magna_graecian_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.90.b # LOCALIZATION KEY - Option B text
        add_political_influence = 200
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.90.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = horde_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = horde_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.91 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.91.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.91.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = IGK
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.91.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = indo_germanic_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = indo_germanic_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.91.b # LOCALIZATION KEY - Option B text
        add_political_influence = 150
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.91.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = horde_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = horde_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.92 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.92.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.92.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = ITA
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.92.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = italo_germanic_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = italo_germanic_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.92.b # LOCALIZATION KEY - Option B text
        add_political_influence = 250
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.92.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = horde_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = horde_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.93 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.93.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.93.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = LOM
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.93.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = lombard_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = lombard_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.93.b # LOCALIZATION KEY - Option B text
        add_political_influence = 100
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.93.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = horde_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = horde_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.94 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.94.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.94.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = MNX
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.94.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = heritage_of_mann 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = modifier_of_mann
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.94.b # LOCALIZATION KEY - Option B text
        add_political_influence = 100
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.94.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = horde_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = horde_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.95 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.95.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.95.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = MXE
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.95.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = heritage_of_imperial_mann 
		}# EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = modifier_of_imperial_mann
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.95.b # LOCALIZATION KEY - Option B text
        add_political_influence = 250
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.95.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = horde_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = horde_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.96 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.96.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.96.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = OGK
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.96.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = ostrogothic_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = ostrogothic_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.96.b # LOCALIZATION KEY - Option B text
        add_political_influence = 250
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.96.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = horde_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = horde_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.97 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.97.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.97.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = VAN
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.97.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = vandal_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = vandal_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.97.b # LOCALIZATION KEY - Option B text
        add_political_influence = 150
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.97.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = horde_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = horde_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.98 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.98.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.98.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = ALX
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.98.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = argead_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = argead_imperial_modifier 
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.98.b # LOCALIZATION KEY - Option B text
        add_political_influence = 300
    }
}

formable_heritage.99 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.99.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.99.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = BHA
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.99.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = bharatavarshi_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = bharatavarshi_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.99.b # LOCALIZATION KEY - Option B text
        add_political_influence = 300
    }
}

formable_heritage.100 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.100.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.100.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = DLA
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.100.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = delian_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = delian_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.100.b # LOCALIZATION KEY - Option B text
        add_political_influence = 250
    }	
}

formable_heritage.101 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.101.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.101.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = HLA
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.101.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = heritage_of_hellas 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = modifier_of_hellas
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.101.b # LOCALIZATION KEY - Option B text
        add_political_influence = 250
    }	
}

formable_heritage.102 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.102.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.102.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = PPN
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.102.a # LOCALIZATION KEY - Option A text
        hidden_effect = {
			set_country_heritage = phoenician_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = phoenician_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.102.b # LOCALIZATION KEY - Option B text
        add_political_influence = 250
    }
}

formable_heritage.103 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.103.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.103.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = PUL
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.103.a # LOCALIZATION KEY - Option A text
        hidden_effect = {
			set_country_heritage = punic_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = punic_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.103.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
}

formable_heritage.104 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.104.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.104.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = ZIQ
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.104.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = ziqanmakom_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = ziqanmakom_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.104.b # LOCALIZATION KEY - Option B text
        add_political_influence = 250
    }	
}

formable_heritage.105 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.105.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.105.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = GRO
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.105.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = graecia_occidentis_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = graecia_occidentis_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.105.b # LOCALIZATION KEY - Option B text
        add_political_influence = 250
    }	
}

# Formable Heritages
formable_heritage.106 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.106.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.106.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = JAL
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.106.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = jalabhumi_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = jalabhumi_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.106.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.106.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = tribal_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.107 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.107.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.107.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = GHB
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.107.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = ghabataleanbar_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = ghabataleanbar_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.107.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.107.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = tribal_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.108 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.108.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.108.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = BRN
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.108.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = brennusovian_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = brennusovia_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.108.b # LOCALIZATION KEY - Option B text
        add_political_influence = 250
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.108.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = horde_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = horde_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.109 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.109.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.109.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = VGK
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.109.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = visigothic_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = visigoth_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.109.b # LOCALIZATION KEY - Option B text
        add_political_influence = 250
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.109.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = horde_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = horde_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.110 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.110.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.110.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = SCO
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.110.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = scottish_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = alba_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.110.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.110.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = tribal_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.111 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.111.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.111.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = CHD
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.111.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = chaldean_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = chaldean_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.111.b # LOCALIZATION KEY - Option B text
        add_political_influence = 250
    }
}

formable_heritage.112 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.112.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.112.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = ADX
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.112.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = adriatic_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = adriatic_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.112.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.112.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = tribal_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.113 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.113.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.113.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = SIC
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.113.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = sicsyrian_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = sicsyrian_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.113.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }	
}

formable_heritage.114 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.114.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.114.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = BAE
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.114.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = baetican_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = baetican_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.114.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }	
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.114.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = tribal_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				#duration = -1
			}
		}
	}		
}

formable_heritage.115 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.115.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.115.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = EAT
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.115.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = erreinu_atlantikoa_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = erreinu_atlantikoa_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.115.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.115.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = tribal_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.125 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.125.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.125.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = QUA
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.125.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = quadia_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = quadia_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.125.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.125.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = tribal_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.126 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.126.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.126.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = GOT
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.126.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = gothia_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = gothia_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.126.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.126.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = tribal_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.127 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.127.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.127.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = BOM
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.127.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = boihaemia_magna_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = boihaemia_magna_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.127.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.127.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = tribal_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.128 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.128.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.128.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = BTN
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.128.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = brittany_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = brittany_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.128.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.128.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = tribal_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.129 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.129.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.129.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = RBR
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.129.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = britannia_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = britannia_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.129.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
	
}

formable_heritage.130 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.130.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.130.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = PAN
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.130.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = pannonia_dacian_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = pannonia_dacian_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.130.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.130.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = tribal_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.131 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.131.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.131.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = PAN
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.131.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = pannonia_celtic_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = pannonia_celtic_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.131.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.131.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = tribal_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				#duration = -1
			}
		}
	}	
}


formable_heritage.132 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.132.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.132.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = PAN
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.132.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = pannonia_germanic_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = pannonia_germanic_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.132.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.132.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = tribal_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.133 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.133.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.133.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = PAN
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.133.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = pannonia_scythian_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = pannonia_scythian_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.133.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.133.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = tribal_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.134 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.134.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.134.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = PAN
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.134.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = pannonia_baltic_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = pannonia_baltic_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.134.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.134.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = tribal_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.135 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.135.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.135.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = EST
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.135.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = estu_imperija_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = estu_imperija_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.135.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.135.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = tribal_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				#duration = -1
			}
		}
	}	
}

formable_heritage.136 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.136.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.136.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = RON
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.136.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = nile_republic_heritage
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = nile_republic_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.136.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
}

formable_heritage.137 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.137.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.137.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = LER
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.137.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = levantine_republic_heritage
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = levantine_republic_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.137.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }
}

formable_heritage.138 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.138.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.138.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = THR
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.138.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = thrace_thracian_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = thrace_thracian_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.138.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }	
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.138.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = tribal_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				#duration = -1
			}
		}
	}
}

formable_heritage.139 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.139.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.139.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = MSW
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.139.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = meshwesh_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = meshwesh_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.139.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }	
}

formable_heritage.140 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.140.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.140.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = HLK
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.140.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = hellenic_kingdom_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = hellenic_kingdom_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.140.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }	
}

formable_heritage.141 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.141.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.141.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = NEM
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.141.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = nemedia_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = nemedia_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.141.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }	
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.141.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = tribal_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				#duration = -1
			}
		}
	}
}

formable_heritage.142 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.142.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.142.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = ARF
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.142.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = arfordir_chwal_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = arfordir_chwal_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.142.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }	
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.142.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = tribal_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				#duration = -1
			}
		}
	}
}

formable_heritage.143 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.143.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.143.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = ISH
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.143.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = ishkuza_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = ishkuza_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.143.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }	
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.143.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = tribal_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				#duration = -1
			}
		}
	}
}

formable_heritage.144 = {
    type = country_event # EVENT TYPE - Can be Country/Province/Character, decides ROOT Scope
    title = formable_heritage.144.t # LOCALIZATION KEY - Title of event
    desc = formable_heritage.144.desc # LOCALIZATION KEY - Description of event
    picture = aqueducts # EVENT PICTURE
    
    left_portrait = current_ruler # LEFT PORTRAIT - Ruler shown at the left hand side in event

    trigger = { # TRIGGER FIELD - Checks if the event will fire at all
    	tag = LIG
    }

    immediate = {
    }

    option = { # EFFECT FIELD - FIRST OPTION
        name = formable_heritage.144.a # LOCALIZATION KEY - Option A text
        hidden_effect = { 
			set_country_heritage = liguria_heritage 
		} # EFFECT
		show_as_tooltip = {
			add_country_modifier = {
				name = liguria_modifier
				#duration = -1
			}
		}
    }
    option = { # EFFECT FIELD - SECOND OPTION
        name = formable_heritage.144.b # LOCALIZATION KEY - Option B text
        add_political_influence = 125
    }	
    option = { # EFFECT FIELD - THIRD OPTION
        name = formable_heritage.144.c # LOCALIZATION KEY - Option B text
		hidden_effect = { 
			set_country_heritage = tribal_heritage 
		}
		show_as_tooltip = {
			add_country_modifier = {
				name = tribal_modifier
				#duration = -1
			}
		}
	}
}