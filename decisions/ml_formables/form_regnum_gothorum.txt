country_decisions = {
	# Regnum Gothorum
	# Designed by Neidhardt, created by Neidhardt
	form_regnum_gothorum = {

		potential = {
			num_of_cities >= 1
			country_culture_group = germanic
			NOT = {
				tag = RGM
			}
			OR = {
				is_ai = no
				num_of_cities >= 100
			}
			NOR = {
				is_tier_2_formable_trigger = yes
				is_endgame_tag_trigger = yes
			}
		}

		highlight = {
			scope:province = {
				OR = {
					is_in_region = scandinavia_region
					is_in_region = elysia_region
					is_in_region = venedia_region
					is_in_region = biebrzanski_region
					is_in_region = vistulia_region
					is_in_region = bohemia_region
				}
			}
		}

		allow = {
			custom_tooltip = {
				text = achaemenid_kingdom_not_exists
				NOT = {
					any_country = {
						tag = ADK
					}
				}
			}
			can_form_nation_trigger = yes
				owns_or_subject_owns_region = scandinavia_region
				owns_or_subject_owns_region = elysia_region
				owns_or_subject_owns_region = venedia_region
				owns_or_subject_owns_region = biebrzanski_region
				owns_or_subject_owns_region = vistulia_region
				owns_or_subject_owns_region = bohemia_region

		}

		effect = {
			custom_tooltip = "tier_1_formable_tooltip"
			change_country_name = "REGNUM_GOTHORUM_NAME"
			if = {
				limit = { is_tribal = yes }
				add_country_modifier = {
					name = civilized_tribe
					duration = 5475
				}
				change_government = tribal_federation
				switch_government_type_event_clearup_effect = yes
			}
			hidden_effect = {
				change_country_adjective = "REGNUM_GOTHORUM_ADJECTIVE"
				change_country_color =  "regnum_gothorum_color"
				change_country_tag = RGM
				change_country_flag = REGNUM_GOTHORUM_FLAG
				trigger_event = {
					id = formable_heritage.190
				}
			}
			add_country_modifier = {
				name = formable_country_huge_modifier
				duration = 7600
			}
			custom_tooltip = southern_tribes_federation_claims_tooltip
			hidden_effect = {
				region:belgica_region = { formable_region_province_effect = yes }
				region:rhaetia_region = { formable_region_province_effect = yes }
				region:celtica_region = { formable_region_province_effect = yes }
			}
			capital_scope = {
				capital_formable_huge_effect = yes
				formable_capital_modifier_large_effect = yes
			}
			add_5_free_province_investments = yes
		}

		ai_will_do = {
			base = 1
		}
	}
}

