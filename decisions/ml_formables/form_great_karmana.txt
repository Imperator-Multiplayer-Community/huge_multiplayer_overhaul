country_decisions = {
	
	# Great Karmana
	# Designed by Neidhardt, created by Neidhardt
	form_great_karmana = {
		
		potential = {
			num_of_cities >= 1
			primary_culture = carmanian
			NOT = {
				tag = GKA
			}
			OR = {
				is_ai = no
				num_of_cities >= 12
			}
			NOR = {
				is_tier_2_formable_trigger = yes
				is_endgame_tag_trigger = yes
			}
		}
		
		highlight = {
			scope:province = {
				OR = {
					is_in_area = carmania_area
					is_in_area = harmozeia_area
					is_in_area = utia_area
					is_in_area = ardashir_area
					is_in_area = drangiana_area
					is_in_area = namek_area
					is_in_area = sagartia_area
					is_in_area = media_felix_area
					is_in_area = gedrosia_area

				}
			}
		}
		
		allow = {
			custom_tooltip = {
				text = great_karmana_not_exists
				NOT = {
					any_country = {
						tag = GKA
					}
				}
			}
			can_form_nation_trigger = yes
				owns_or_subject_owns_area = carmania_area
				owns_or_subject_owns_area = harmozeia_area
				owns_or_subject_owns_area = utia_area
				owns_or_subject_owns_area = ardashir_area
				owns_or_subject_owns_area = drangiana_area
				owns_or_subject_owns_area = namek_area
				owns_or_subject_owns_area = sagartia_area
				owns_or_subject_owns_area = media_felix_area
				owns_or_subject_owns_area = gedrosia_area

		}
		
		effect = {
			custom_tooltip = "tier_2_formable_tooltip"
			change_country_name = "GREAT_KARMANA_NAME"
			hidden_effect = {
				change_country_adjective = "GREAT_KARMANA_ADJECTIVE"
				change_country_color = "great_karmana_color"
				change_country_tag = GKA
				change_country_flag = GREAT_KARMANA_FLAG
				trigger_event = {
					id = formable_heritage.183
				}				
			}
			custom_tooltip = great_karmana_claims_tooltip
			hidden_effect = {
				region:palestine_region = { formable_region_province_effect = yes }
				region:assyria_region = { formable_region_province_effect = yes }
			}
			add_3_free_province_investments = yes
			hidden_effect = {
			}
			capital_scope = {
				establish_city_effect = yes
			}
			capital_scope = {
					capital_formable_large_effect = yes
					formable_capital_modifier_normal_effect = yes
			}
		}
		
		ai_will_do = {
			base = 1
		}
	}

}
